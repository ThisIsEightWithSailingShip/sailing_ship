// document.querySelectorAll('.store-card').forEach(function(card) {
//     card.addEventListener('click', function() {
//         let storeId = card.getAttribute("storeId"); // 데이터 속성으로부터 storeId를 읽어옴
//         // 페이지 이동
//         console.log(storeId)
//         let id = parseInt(storeId);
//         window.location.href = `/sail/stores/${id}`;
//     });
// });